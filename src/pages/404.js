import React from "react";
import Layout from "../components/Layout";

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>Side ikke fundet</h1>
      <p>Du har søgt på en side som ikke findes.</p>
    </div>
  </Layout>
);

export default NotFoundPage;
